FROM nginx:1.25.4-alpine-slim

ARG NGINX_NAME

ARG NGINX_SERVER_NAME
ENV NGINX_SERVER_NAME=${{secrets.NGINX_SERVER_NAME}}

ARG NGINX_SERVER_HOST
ENV NGINX_SERVER_HOST=${{secrets.NGINX_SERVER_HOST}}

COPY ${NGINX_NAME} /etc/nginx/templates/default.conf.template
